package HW01.regular;

public class Decorator {
    /**
     * Выводит строку с результатом
     */
    public static String decorate (int a) {
        return String.format("Result is: %d.", a);
    }
}
